<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= event.title %> - <%= settings.site_title %></title>
</head>
<body>

    <header>
        <h1><%= event.title %></h1>
        <p>Organized by: <strong><%= settings.author_name %></strong></p>
        <a href="/">← Back to All Events</a>
    </header>

    <hr>

    <main>
        <section>
            <h3>About this Event</h3>
            <p><strong>Description:</strong> <%= event.description %></p>
            <div>
                <h4>Full Details:</h4>
                <p><%= event.content %></p>
            </div>
            <p><strong>Ticket Price:</strong> $<%= event.price_tickets %></p>
        </section>

        <hr>

        <section>
            <h3>Register Your Interest</h3>
            <p>Fill in your details to secure a spot at this event.</p>
            
            <form action="/register-attendee" method="POST">
                <input type="hidden" name="event_id" value="<%= event.id %>">

                <div>
                    <label for="name">Full Name:</label><br>
                    <input type="text" id="name" name="attendee_name" placeholder="Enter your name" required>
                </div>
                <br>
                <div>
                    <label for="email">Email Address:</label><br>
                    <input type="email" id="email" name="attendee_email" placeholder="email@example.com" required>
                </div>
                <br>
                <button type="submit">Confirm Registration</button>
            </form>
        </section>
    </main>

    <footer>
        <p>&copy; 2026 <%= settings.site_title %></p>
    </footer>

</body>
</html>
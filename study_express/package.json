{
  "dependencies": {
    "dotenv": "^17.2.3",
    "ejs": "^3.1.10",
    "express": "^5.2.1",
    "sqlite": "^5.1.1",
    "sqlite3": "^5.1.7"
  },
  "scripts": {
    "start": "node index",
    "dev": "node --watch --env-file=.env index",
    "build-db": "sqlite3 database.db < db_schema.sql",
    "clean-db": "del database.db"
  }
}
